<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <style>
    body {
      font-family: Arial, sans-serif;
      padding: 20px;
      background: #f1f1f1;
    }
    .card {
      background: white;
      border-radius: 10px;
      padding: 20px;
      box-shadow: 0 0 10px rgba(0,0,0,0.1);
      max-width: 400px;
      margin: auto;
    }
    .card img {
      width: 120px;
      height: auto;
      display: block;
      margin: 0 auto 10px;
    }
    h1 {
      text-align: center;
    }
  </style>
</head>
<body>
  <div class="card">
    <img src="{{foto}}" alt="Foto del ciudadano">
    <h1>Datos RENIEC</h1>
    <p><strong>DNI:</strong> {{dni}}</p>
    <p><strong>Nombre Completo:</strong> {{nombreCompleto}}</p>
    <p><strong>Domicilio:</strong> {{domicilio}}</p>
    <p><strong>Estado Civil:</strong> {{estadoCivil}}</p>
  </div>
</body>
</html>
